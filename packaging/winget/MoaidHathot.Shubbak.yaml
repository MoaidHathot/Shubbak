# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.12.0.schema.json
# The three files in this directory are templates: InstallerSha256, ProductCode and
# ReleaseDate are filled by tools/build-release.ps1 from a real build, and the filled
# copies are what gets submitted. See RELEASING.md.

PackageIdentifier: MoaidHathot.Shubbak
PackageVersion: 0.10.0
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.12.0
